CREATE TABLE IF NOT EXISTS my_table (
    full_name TEXT,
    gender TEXT,
    location TEXT,
    city TEXT,
    country TEXT,
    postcode VARCHAR(255),
    latitude FLOAT,
    longitude FLOAT,
    email TEXT
);
